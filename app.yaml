runtime: php72  # language of the app

runtime_config:
  document_root: public   #folder where index.php is# Ensure we skip ".env", which is only for local development

env_variables:
  # Put production environment variables here.
  APP_ENV: local   # or production
  APP_DEBUG : true # or false
  APP_KEY: base64:NduRBmjmzhWVqJSo9Bzs0FCEAPJEA4cPvTY1Il4KhjY=
  # go to generate app key paragraf in this tutorial CACHE_DRIVER: database# instead of putting the cache in the database I recommend using redis
  SESSION_DRIVER: database #or file since both work APP_LOG: daily
  APP_TIMEZONE: UTC #your timezone of choice

  # follow the part of the tutorial on setting up your SQL database
  DB_CONNECTION: mysql
  DB_HOST: localhost
  DB_DATABASE: instance_id
  DB_USERNAME: root
  DB_PASSWORD: 123456
  DB_SOCKET: /cloudsql/instance_name
  QUEUE_DRIVER: database # in case you execute queued jobs

  MAIL_DRIVER: smtp
  MAIL_HOST: smtp.mailtrap.io
  MAIL_PORT: 2525
  MAIL_USERNAME: 6b1641278ec061
  MAIL_PASSWORD: 2ce23188e4f893

  LOG_DELETE:  true # this is a parameter added by us in the project .env file. You can add here any setting you would add to your .env file GOOGLE_VISION_PROJECT_ID : project_id
